#include <stdio.h>
#include <math.h>
int main()
{
	 float a;
	 float root;
	 printf("Enter a number: \n");
	 scanf("%f",&a);
	 root=sqrt(a);
	 printf("Square root of %f is %f",a,root);
	 return 0;
}
